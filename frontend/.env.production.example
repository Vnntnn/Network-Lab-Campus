# Optional. By default frontend uses relative /api/v1 on the current host.
VITE_API_BASE_URL=/api/v1

# Optional. By default frontend uses current host for websocket URLs.
# Example: wss://app.example.com
VITE_WS_BASE_URL=
